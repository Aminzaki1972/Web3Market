"use strict";
(async function(){
 const root=document.querySelector('#sellerGrid');
 const sb=window.Web3MarketSupabase?.getClient?.()||window.supabaseClient||window.web3marketSupabase;
 if(!root)return;
 if(!sb){root.innerHTML='<div class="seller-banner">⚠ Database connection unavailable.</div>';return;}
 const {data:{user},error:authError}=await sb.auth.getUser();
 if(authError||!user){location.replace('login.html?next=seller-dashboard.html');return;}
 const esc=v=>String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));
 const money=(v,c)=>v==null?'—':new Intl.NumberFormat('en-US',{style:'currency',currency:c||'USD',maximumFractionDigits:0}).format(Number(v));
 const {data:profile,error:profileError}=await sb.from('profiles').select('display_name,email,bio,wallet_address,role').eq('id',user.id).maybeSingle();
 if(profileError||String(profile?.role||'').toLowerCase()!=='seller'){location.replace('marketplace.html');return;}
 const projectsResult=await sb.from('projects').select('id,title,status,price,currency,created_at').eq('owner_id',user.id).order('created_at',{ascending:false});
 const projects=projectsResult.error?[]:(projectsResult.data||[]);
 const ids=projects.map(p=>p.id);let deals=[];
 if(ids.length){const r=await sb.from('deals').select('id,project_id,buyer_id,amount,currency,status,created_at').in('project_id',ids).order('created_at',{ascending:false});if(!r.error)deals=r.data||[];}
 const completed=deals.filter(d=>['completed','released','closed'].includes(String(d.status||'').toLowerCase()));
 const pending=deals.filter(d=>!['completed','released','closed','cancelled','rejected'].includes(String(d.status||'').toLowerCase()));
 const revenue=completed.reduce((s,d)=>s+Number(d.amount||0),0);
 const active=projects.filter(p=>['published','active','live'].includes(String(p.status||'').toLowerCase())).length;
 const initials=(profile.display_name||user.email||'S').split(/[\s@._-]+/).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase();
 const name=esc(profile.display_name||'Web3 Seller');
 const email=esc(profile.email||user.email||'');
 const wallet=profile.wallet_address||'';
 const walletLabel=wallet?wallet.slice(0,6)+'…'+wallet.slice(-4):'Not connected';
 const byId=new Map(projects.map(p=>[p.id,p]));
 const status=d=>String(d.status||'pending').toLowerCase();
 const offerActions=d=>status(d)==='pending'?`<div style="display:flex;gap:7px;flex-wrap:wrap;margin-top:9px"><button class="dash-btn primary" data-accept="${esc(d.id)}">✓ Accept Offer</button><button class="dash-btn" data-reject="${esc(d.id)}">✕ Reject</button></div>`:'';
 root.innerHTML=`<div class="dash-top"><div><h1>Seller Dashboard</h1><p>Manage your Web3 business from one professional workspace.</p></div><div class="dash-actions"><a class="dash-btn" href="marketplace.html">View Marketplace</a><a class="dash-btn primary" href="sell-project.html">＋ Add Listing</a></div></div><div class="seller-banner">✦ Your seller workspace is ready — manage listings, deals, reputation and payouts securely.</div><section class="profile-card"><div class="avatar">${esc(initials||'S')}</div><div class="profile"><h2>${name}<span class="verified">✓ Verified role</span></h2><p>${email}</p></div><a class="dash-btn" href="verification.html">Manage profile</a></section><section class="stats"><div class="stat"><div class="label">TOTAL REVENUE</div><div class="value">${money(revenue,'USD')}</div><div class="sub">Completed deals</div></div><div class="stat"><div class="label">ACTIVE LISTINGS</div><div class="value">${active}</div><div class="sub">${projects.length} total</div></div><div class="stat"><div class="label">ORDERS & OFFERS</div><div class="value">${deals.length}</div><div class="sub">${pending.length} awaiting action</div></div><div class="stat"><div class="label">COMPLETED DEALS</div><div class="value">${completed.length}</div><div class="sub">Successful transactions</div></div></section><section class="seller-grid"><div><div class="panel" id="listings"><h2>My Listings</h2>${projects.length?projects.slice(0,8).map(p=>`<article class="listing"><div><h3><a href="project.html?id=${encodeURIComponent(p.id)}">${esc(p.title||'Untitled project')}</a></h3><div class="muted">${money(p.price,p.currency)} · ${esc(p.created_at?new Date(p.created_at).toLocaleDateString():'')}</div></div><span class="status-pill">${esc(p.status||'draft')}</span></article>`).join(''):'<div class="empty">No listings yet.<br><br><a class="dash-btn primary" href="sell-project.html">Create your first listing</a></div>'}</div><div class="panel" id="deals"><h2>Orders & Deals</h2>${deals.length?deals.slice(0,8).map(d=>`<article class="listing"><div><h3>${esc(byId.get(d.project_id)?.title||'Project')}</h3><div class="muted">${money(d.amount,d.currency)} · ${d.created_at?new Date(d.created_at).toLocaleDateString():''}</div>${offerActions(d)}</div><span class="status-pill">${esc(d.status||'pending')}</span></article>`).join(''):'<div class="empty">No orders or offers yet. New buyer activity will appear here.</div>'}</div><div class="panel" id="earnings"><h2>Earnings</h2><div class="value" style="font-size:28px;font-weight:900">${money(revenue,'USD')}</div><p class="muted">Completed deal revenue tracked by Web3Market.</p></div></div><aside><div class="panel"><h2>Seller Profile</h2><div class="profile-row"><div class="avatar">${esc(initials||'S')}</div><div><strong>${name}</strong><div class="muted">${email}</div></div></div>${profile.bio?`<p class="muted" style="margin-top:13px">${esc(profile.bio)}</p>`:''}</div><div class="panel" id="reviews"><h2>Reputation</h2><div class="score">${completed.length?Math.min(100,70+completed.length*3):70}/100</div><div class="muted">Seller reputation</div><div class="progress"><span style="width:${completed.length?Math.min(100,70+completed.length*3):70}%"></span></div><div class="muted">Your reputation grows with successful transactions and verified reviews.</div></div><div class="panel"><h2>Seller Tools</h2><div class="quick-links"><a href="sell-project.html">＋ New Listing</a><a href="marketplace.html">🛍 Marketplace</a><a href="verification.html">✓ Verification</a><a href="escrow-testnet.html">🔐 Escrow</a></div></div><div class="panel" id="wallet"><h2>Wallet & Payouts</h2><p class="muted">${esc(walletLabel)}</p><p class="muted">${wallet?'Wallet connected to your seller profile.':'Connect a wallet before enabling live withdrawals.'}</p><a class="dash-btn" href="verification.html">Manage Wallet</a></div><div class="panel" id="messages"><h2>Messages</h2><div class="empty">No new messages.</div></div></aside></section>`;
 const sideWallet=document.getElementById('sideWallet');if(sideWallet)sideWallet.textContent=walletLabel;
 async function offerAction(id,action){const {data:{session}}=await sb.auth.getSession();if(!session)throw new Error('Please sign in again.');const r=await fetch('https://hzhqlexnhtukfljcvnyd.supabase.co/functions/v1/seller-offer-action',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+session.access_token},body:JSON.stringify({deal_id:id,action})});const j=await r.json().catch(()=>({error:'Invalid server response'}));if(!r.ok||!j.ok)throw new Error(j.error||'Offer action failed');return j;}
 root.querySelectorAll('[data-accept]').forEach(btn=>btn.addEventListener('click',async()=>{const id=btn.dataset.accept;if(!confirm('Accept this 80 USD offer and create the deal?'))return;btn.disabled=true;try{await offerAction(id,'accept');alert('Offer accepted successfully.');location.reload();}catch(e){btn.disabled=false;alert('Offer action error: '+(e.message||'Unable to accept offer.'));}}));
 root.querySelectorAll('[data-reject]').forEach(btn=>btn.addEventListener('click',async()=>{const id=btn.dataset.reject;if(!confirm('Reject this offer?'))return;btn.disabled=true;try{await offerAction(id,'reject');alert('Offer rejected.');location.reload();}catch(e){btn.disabled=false;alert('Offer action error: '+(e.message||'Unable to reject offer.'));}}));
})();
