"use strict";
(async function(){
 const root=document.querySelector('#checkoutApp');
 const sb=window.Web3MarketSupabase?.getClient?.()||window.supabaseClient||window.web3marketSupabase;
 const dealId=new URLSearchParams(location.search).get('deal');
 if(!root||!sb||!dealId){if(root)root.innerHTML='<div class="status">Deal information is unavailable.</div>';return;}
 const {data:{user},error:authError}=await sb.auth.getUser();
 if(authError||!user){root.innerHTML='<div class="status">Please sign in.</div>';return;}
 const {data:deal,error}=await sb.from('deals').select('id,project_id,buyer_id,seller_id,amount,currency,status,platform_fee_percent,platform_fee,platform_fee_amount,seller_net_amount,fee_locked_at,payment_status,payment_tx_hash,chain_id,safe_address,token_contract,token_symbol').eq('id',dealId).maybeSingle();
 if(error||!deal){root.innerHTML='<div class="status">Deal not found.</div>';return;}
 if(deal.buyer_id!==user.id&&deal.seller_id!==user.id){root.innerHTML='<div class="status">You are not a participant in this deal.</div>';return;}
 const esc=v=>String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));
 const amount=Number(deal.amount||0);const fee=Number(deal.platform_fee_amount??deal.platform_fee??amount*0.075);const net=Number(deal.seller_net_amount??(amount-fee));const currency=esc(deal.currency||deal.token_symbol||'USDT');const pct=Number(deal.platform_fee_percent||7.5);const configured=Number(deal.chain_id)===56&&/^0x[0-9a-fA-F]{40}$/.test(String(deal.safe_address||''))&&/^0x[0-9a-fA-F]{40}$/.test(String(deal.token_contract||''));
 const paymentState=String(deal.payment_status||'pending').toLowerCase();
 root.innerHTML=`<h2>Deal Checkout</h2><div class="card"><p>Deal amount: <strong>${amount.toLocaleString()} ${currency}</strong></p><p>Web3Market fee: <strong>${fee.toLocaleString()} ${currency}</strong> (${pct.toFixed(2)}%)</p><p>Seller net amount: <strong>${net.toLocaleString()} ${currency}</strong></p><p>Status: <strong>${esc(deal.status)}</strong></p><p>Payment: <strong>${esc(paymentState)}</strong>${deal.payment_tx_hash?` · <code>${esc(deal.payment_tx_hash)}</code>`:''}</p>${configured&&deal.buyer_id===user.id&&!['confirmed','paid'].includes(paymentState)&&!deal.payment_tx_hash?'<button class="btn primary" id="payBtn">Pay from connected wallet</button>':'<div class="status">'+(configured?'Payment is submitted or already verified.':'Live payment is not enabled for this deal yet. The platform must assign a BNB Smart Chain Safe and token contract before funds can move.')+'</div>'}<a class="btn" href="deal-room.html?deal=${encodeURIComponent(deal.id)}">Open Deal Room</a></div>`;
 const payBtn=document.getElementById('payBtn');if(!payBtn)return;
 payBtn.addEventListener('click',async()=>{const old=payBtn.textContent;payBtn.disabled=true;try{if(!window.ethereum)throw new Error('Open Web3Market in MetaMask or another compatible wallet.');let chain=await window.ethereum.request({method:'eth_chainId'});if(chain!=='0x38'){await window.ethereum.request({method:'wallet_switchEthereumChain',params:[{chainId:'0x38'}]});chain=await window.ethereum.request({method:'eth_chainId'});}if(chain!=='0x38')throw new Error('Please switch to BNB Smart Chain.');const accounts=await window.ethereum.request({method:'eth_requestAccounts'});const from=accounts?.[0];if(!from)throw new Error('No wallet account selected.');const decimalsHex=await window.ethereum.request({method:'eth_call',params:[{to:deal.token_contract,data:'0x313ce567'},'latest']});const decimals=Number(BigInt(decimalsHex));if(!Number.isInteger(decimals)||decimals<0||decimals>36)throw new Error('Could not determine token decimals.');const parts=String(deal.amount).split('.');const whole=BigInt(parts[0]||'0');const frac=(parts[1]||'').padEnd(decimals,'0');if(frac.length>decimals)throw new Error('Deal amount has too many decimal places for this token.');const base=(whole*(10n**BigInt(decimals))+BigInt(frac||'0')).toString(16).padStart(64,'0');const to=String(deal.safe_address).toLowerCase().replace(/^0x/,'').padStart(64,'0');const data='0xa9059cbb'+to+base;payBtn.textContent='Confirm payment in wallet…';const txHash=await window.ethereum.request({method:'eth_sendTransaction',params:[{from,to:deal.token_contract,data}]});if(!/^0x[0-9a-fA-F]{64}$/.test(txHash))throw new Error('Wallet returned an invalid transaction hash.');payBtn.textContent='Verifying payment on BNB…';const {data:{session}}=await sb.auth.getSession();if(!session)throw new Error('Session expired. Please sign in again.');const response=await fetch('https://hzhqlexnhtukfljcvnyd.supabase.co/functions/v1/verify-deal-payment',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+session.access_token},body:JSON.stringify({deal_id:deal.id,tx_hash:txHash})});const result=await response.json().catch(()=>({error:'Invalid verifier response'}));if(!response.ok||!result.ok)throw new Error(result.error||'Payment verification failed');alert('Payment confirmed on-chain.');location.reload();}catch(err){payBtn.disabled=false;payBtn.textContent=old;alert(err?.message||'Payment failed.')}});
})();
